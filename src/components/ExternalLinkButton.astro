---
import Button from "./Button.astro";
const { id, label, disabled, class: className, style, url } = Astro.props
---

<a href={url} target='_blank'>
  <Button
    id={id}
    label={label}
    class={className}
    disabled={disabled}
    style={style}
  />
</a>